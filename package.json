{
  "type": "module",
  "name": "ssupwithstates",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "npm run build:data && vite",
    "build": "npm run validate && npm run build:data && vite build",
    "build:data": "node scripts/build-data.mjs",
    "validate": "node scripts/validate.mjs",
    "geo": "node scripts/simplify-geo.mjs",
    "test": "vitest run",
    "build:state": "node scripts/build-state.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^7.3.6",
    "vitest": "^3.2.7"
  }
}
